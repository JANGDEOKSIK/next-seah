/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  // basePath: "/next-seah",
  // assetPrefix: "/next-seah/",
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
